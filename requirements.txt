# ─── Nube Privada de Camila — Dependencias Fijadas (M5) ───
# Versiones exactas para reproducibilidad y seguridad.
# Actualizar con: pip install --upgrade <paquete> && pip freeze | grep <paquete>
# Escanear vulnerabilidades con: pip-audit

# Core
fastapi==0.139.0
uvicorn==0.51.0
starlette==1.3.1
pydantic==2.13.4
python-multipart==0.0.32

# Google Drive API
google-api-python-client==2.198.0
google-auth==2.55.1
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.1

# Utilidades
python-dotenv==1.2.2
requests==2.34.2

# Seguridad / 2FA
pyotp==2.10.0
qrcode==8.2
